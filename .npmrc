# Expo + pnpm monorepo: hoisted node_modules so Expo's gradle plugins
# (which use relative paths into node_modules) can resolve correctly.
# See: https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
