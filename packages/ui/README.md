# @seamflow/ui

Shared React component library used by `seamflow-web` and `seamflow-admin` (buttons, forms, modals, design system). Mobile components live inside `seamflow-app/src/components` because they're React Native, not React DOM.

Not implemented yet.
