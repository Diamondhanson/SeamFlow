{
  "name": "@seamflow/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed API client SDK consumed by seamflow-app, seamflow-web, and seamflow-admin.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@seamflow/schemas": "workspace:*",
    "@seamflow/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
