# @seamflow/api-client

Typed API client SDK consumed by `seamflow-app`, `seamflow-web`, and `seamflow-admin`. Wraps the HTTP layer so frontends call `apiClient.orders.create(...)` instead of raw fetch.

Not implemented yet.
