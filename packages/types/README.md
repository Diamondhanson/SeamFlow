# @seamflow/types

Shared TypeScript types for SeamFlow. Defines the canonical shape of `Client`, `Order`, `Measurements`, `OrderStatus`, etc. — imported by every app so the data model is defined exactly once.

Not implemented yet.
