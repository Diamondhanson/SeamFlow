{
  "name": "@seamflow/schemas",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod schemas for runtime validation across SeamFlow apps.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "echo 'no tests for @seamflow/schemas' && exit 0",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
