# Where the NestJS API lives — used by the public order page (server-side fetch).
# Dev: http://localhost:3001
# Prod: https://api.seamflowtech.com (eventually)
SEAMFLOW_API_URL=http://localhost:3001
