{
  "name": "seamflow",
  "version": "0.1.0",
  "private": true,
  "description": "SeamFlow monorepo — tailor CRM mobile app, client-facing web, admin dashboard, and backend API.",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "pnpm": {
    "patchedDependencies": {
      "@supabase/supabase-js@2.106.0": "patches/@supabase__supabase-js@2.106.0.patch"
    }
  }
}
